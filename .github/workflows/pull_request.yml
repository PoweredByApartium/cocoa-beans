name: Pull request CI

on:
  pull_request

jobs:
  unit_test:
    uses: PoweredByApartium/cocoa-beans/.github/workflows/callable.test.yml@main

  writerside_test:
    uses: PoweredByApartium/cocoa-beans/.github/workflows/callable.writerside.yml@main
      